.\" Automatically generated by Pandoc 3.11
.\"
.TH "VINPUT" "1" "2024年8月" "fcitx5\-vinput" "fcitx5\-vinput 用户手册"
.SH 名称
vinput \- fcitx5\-vinput 命令行控制与管理工具
.SH 语法
\f[B]vinput\f[R] [\f[I]全局选项\f[R]] \f[I]命令\f[R] [\f[I]子命令\f[R]]
[\f[I]选项\f[R]]
.SH 描述
\f[B]vinput\f[R] 是 \f[B]fcitx5\-vinput\f[R]
的命令行客户端。\f[B]fcitx5\-vinput\f[R] 是为 Fcitx5
输入法框架开发的离线/云端双模语音输入插件。
.PP
\f[B]vinput\f[R] 提供了对离线语音识别模型、云端 ASR 脚本、LLM
后处理提供商及适配器、识别场景、音频采集设备、热词表、守护进程生命周期以及录音状态的完整控制。
.SH 全局选项
.TP
\f[B]\-h\f[R], \f[B]\-\-help\f[R]
显示帮助信息并退出。
.TP
\f[B]\-v\f[R], \f[B]\-\-version\f[R]
显示程序版本并退出。
.TP
\f[B]\-j\f[R], \f[B]\-\-json\f[R]
以 JSON 格式输出结果，便于脚本解析。
.SH 命令
.SS 初始化
.TP
\f[B]init\f[R] [\f[B]\-f\f[R], \f[B]\-\-force\f[R]]
初始化默认配置文件及相关目录（创建于 \f[I]\(ti/.config/vinput\f[R] 和
\f[I]\(ti/.local/share/vinput\f[R]）。若指定
\f[B]\-f\f[R]，则会覆盖已有配置。
.SS 本地模型管理
管理基于 \f[B]sherpa\-onnx\f[R] 的离线语音识别模型（在 Lite
轻量版中不可用）。
.TP
\f[B]model list\f[R] [\f[B]\-a\f[R], \f[B]\-\-available\f[R]]
列出本地已安装的模型。若附加 \f[B]\-a\f[R] 或
\f[B]\-\-available\f[R]，则从在线注册表检索可用模型列表。
.TP
\f[B]model add\f[R] \f[I]ID\f[R]
从注册表下载并安装指定短 \f[I]ID\f[R] 的离线模型。
.TP
\f[B]model use\f[R] \f[I]ID\f[R]
将指定模型设为当前激活的离线 ASR 模型。
.TP
\f[B]model remove\f[R] \f[I]ID\f[R]
卸载指定的本地模型。
.TP
\f[B]model info\f[R] \f[I]ID\f[R]
查看指定模型的详细元数据与配置信息。
.SS 云端 ASR 服务商管理
管理第三方云端语音识别脚本（如豆包、阿里云百炼、ElevenLabs、OpenAI
兼容接口等）。
.TP
\f[B]provider list\f[R] [\f[B]\-a\f[R], \f[B]\-\-available\f[R]]
列出本地已配置的 ASR 服务商。若指定
\f[B]\-a\f[R]，则浏览在线注册表中可用的服务商脚本。
.TP
\f[B]provider add\f[R] \f[I]ID\f[R]
从注册表安装指定 \f[I]ID\f[R] 的服务商脚本。
.TP
\f[B]provider use\f[R] \f[I]ID\f[R]
将激活的 ASR 服务商切换为 \f[I]ID\f[R]。
.TP
\f[B]provider edit\f[R] \f[I]ID\f[R]
在默认文本编辑器中打开该服务商的配置或脚本。
.TP
\f[B]provider remove\f[R] \f[I]ID\f[R]
卸载指定的 ASR 服务商。
.SS LLM 润色服务商管理
管理用于场景后处理、错别字修正和文字润色的 OpenAI 兼容大模型接口。
.TP
\f[B]llm list\f[R]
列出所有已配置的 LLM 服务商。
.TP
\f[B]llm add\f[R] \f[I]ID\f[R] \f[B]\-u\f[R], \f[B]\-\-base\-url\f[R] \f[I]URL\f[R] [\f[B]\-k\f[R], \f[B]\-\-api\-key\f[R] \f[I]KEY\f[R]] [\f[B]\-e\f[R], \f[B]\-\-extra\-body\f[R] \f[I]JSON\f[R]]
添加新的 LLM 服务商，指定 Base URL、API
Key（可选）及额外的请求体参数（可选）。
.TP
\f[B]llm edit\f[R] \f[I]ID\f[R] [\f[B]\-u\f[R], \f[B]\-\-base\-url\f[R] \f[I]URL\f[R]] [\f[B]\-k\f[R], \f[B]\-\-api\-key\f[R] \f[I]KEY\f[R]] [\f[B]\-e\f[R], \f[B]\-\-extra\-body\f[R] \f[I]JSON\f[R]]
修改已有 LLM 服务商的连接配置。
.TP
\f[B]llm remove\f[R] \f[I]ID\f[R]
删除指定的 LLM 服务商。
.TP
\f[B]llm test\f[R] \f[I]ID\f[R]
测试与指定 LLM 服务商的网络连通性及 API 认证是否有效。
.SS LLM 适配器管理
管理将非标准本地模型封装为 OpenAI 兼容接口的本地桥接适配进程。
.TP
\f[B]adapter list\f[R] [\f[B]\-a\f[R], \f[B]\-\-available\f[R]]
列出本地适配器（显示自启动状态）；使用 \f[B]\-a\f[R]
浏览在线注册表中的可用适配器。
.TP
\f[B]adapter ps\f[R]
列出适配器进程与运行时状态（包括运行状态、PID、自启动策略与启动命令）。
.TP
\f[B]adapter status\f[R] \f[I]ID\f[R]
查看指定适配器的详细运行状态与配置属性。
.TP
\f[B]adapter add\f[R] \f[I]ID\f[R]
安装指定 \f[I]ID\f[R] 的适配器脚本。
.TP
\f[B]adapter start\f[R] \f[I]ID\f[R]
启动后台适配器进程。
.TP
\f[B]adapter stop\f[R] \f[I]ID\f[R]
停止正在运行的适配器进程。
.TP
\f[B]adapter restart\f[R] \f[I]ID\f[R]
重启指定的适配器进程。
.TP
\f[B]adapter enable\f[R] \f[I]ID\f[R]
将指定适配器设置为随守护进程自启动。
.TP
\f[B]adapter disable\f[R] \f[I]ID\f[R]
取消指定适配器随守护进程自启动。
.SS 热词表管理
管理用于增强离线 sherpa\-onnx 模型专有名词识别准确率的自定义词表（在
Lite 轻量版中不可用）。
.TP
\f[B]hotword get\f[R]
显示当前生效的热词文件路径。
.TP
\f[B]hotword set\f[R] \f[I]PATH\f[R]
设置热词文本文件路径。
.TP
\f[B]hotword clear\f[R]
清除已配置的热词文件路径。
.TP
\f[B]hotword edit\f[R]
在默认文本编辑器中编辑热词文件。
.SS 音频设备管理
.TP
\f[B]device list\f[R]
列出系统中可用的 PipeWire 录音设备。
.TP
\f[B]device use\f[R] \f[I]ID\f[R]
设置当前用于录音的输入设备（使用设备节点名称或 \f[I]default\f[R]）。
.SS 场景管理
场景（Scene）定义了发送给 LLM 的提示词模板（Prompt）及相关参数，用于对
ASR 识别出的原始文本进行重写与润色。
.TP
\f[B]scene list\f[R]
列出所有已定义的场景。
.TP
\f[B]scene add\f[R] \f[B]\-\-id\f[R] \f[I]ID\f[R] [\f[B]\-l\f[R], \f[B]\-\-label\f[R] \f[I]LABEL\f[R]] [\f[B]\-t\f[R], \f[B]\-\-prompt\f[R] \f[I]PROMPT\f[R]] [\f[B]\-p\f[R], \f[B]\-\-provider\f[R] \f[I]PROVIDER_ID\f[R]] [\f[B]\-m\f[R], \f[B]\-\-model\f[R] \f[I]MODEL\f[R]] [\f[B]\-c\f[R], \f[B]\-\-count\f[R] \f[I]N\f[R]] [\f[B]\-\-show\-raw\f[R]|\f[B]\-\-no\-show\-raw\f[R]]
添加新的后处理场景。\f[I]N\f[R] 表示向 LLM
请求的不同改写结果最大数量；\f[CR]0\f[R] 表示该场景不使用 LLM。使用
\f[B]\-\-no\-show\-raw\f[R]
可在候选列表中排除原始语音转写文本，从而在单改写结果时直接上屏。
.TP
\f[B]scene edit\f[R] \f[I]ID\f[R] [\f[I]选项\f[R]]
修改已有场景配置。
.TP
\f[B]scene use\f[R] \f[I]ID\f[R]
切换当前激活的场景。
.TP
\f[B]scene remove\f[R] \f[I]ID\f[R]
删除场景（\f[CR]__raw__\f[R] 等内置基础场景不可删除）。
.SS 配置读写
.TP
\f[B]config get\f[R] \f[I]POINTER\f[R]
通过 JSON Pointer 路径读取配置值（例如 \f[CR]/global/capture_device\f[R]
或 \f[CR]/asr/input_gain\f[R]）。
.TP
\f[B]config set\f[R] \f[I]POINTER\f[R] [\f[I]VALUE\f[R]] [\f[B]\-i\f[R], \f[B]\-\-stdin\f[R]]
写入指定 JSON Pointer 路径的配置值。
.TP
\f[B]config edit\f[R] \f[I]TARGET\f[R]
在 \f[CR]$EDITOR\f[R] 中打开配置文件。\f[I]TARGET\f[R] 必须为
\f[B]core\f[R]（\f[I]\(ti/.config/vinput/config.json\f[R]）或
\f[B]fcitx\f[R]（\f[I]\(ti/.config/fcitx5/conf/vinput.conf\f[R]）。
.SS 守护进程控制
.TP
\f[B]daemon status\f[R]
查看 \f[B]vinput\-daemon\f[R] 的运行状态（PID、活动模型、D\-Bus
连接等）。
.TP
\f[B]daemon start\f[R]
通过 systemd user 服务启动守护进程。
.TP
\f[B]daemon stop\f[R]
停止守护进程。
.TP
\f[B]daemon restart\f[R]
重启守护进程。
.SS 录音控制
.TP
\f[B]recording start\f[R]
开始录音。
.TP
\f[B]recording stop\f[R] [\f[B]\-s\f[R], \f[B]\-\-scene\f[R] \f[I]SCENE_ID\f[R]]
结束录音，触发语音识别并按指定场景进行后处理。
.TP
\f[B]recording toggle\f[R] [\f[B]\-s\f[R], \f[B]\-\-scene\f[R] \f[I]SCENE_ID\f[R]]
切换录音状态（开始/停止）。
.SH 文件
.TP
\f[I]\(ti/.config/vinput/config.json\f[R]
核心守护进程、ASR、LLM 与场景配置文件。
.TP
\f[I]\(ti/.config/fcitx5/conf/vinput.conf\f[R]
Fcitx5 输入法插件配置文件（按键快捷键、候选词数量、界面设置等）。
.TP
\f[I]\(ti/.local/share/vinput/models/\f[R]
已安装的离线 sherpa\-onnx 模型存放目录。
.TP
\f[I]\(ti/.local/share/vinput/providers/\f[R]
已安装的云端 ASR 服务商脚本目录。
.TP
\f[I]\(ti/.local/share/vinput/adapters/\f[R]
已安装的 LLM 适配器脚本目录。
.SH 另请参阅
\f[B]vinput\-daemon\f[R](1), \f[B]vinput\-config\f[R](5),
\f[B]fcitx5\f[R](1), \f[B]systemctl\f[R](1)
