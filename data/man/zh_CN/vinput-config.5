.\" Automatically generated by Pandoc 3.11
.\"
.TH "VINPUT\-CONFIG" "5" "2024年8月" "fcitx5\-vinput" "fcitx5\-vinput 文件格式"
.SH 名称
vinput\-config \- fcitx5\-vinput 配置文件格式与选项说明
.SH 描述
\f[B]fcitx5\-vinput\f[R] 使用两份主要的配置文件：
.IP "1." 3
\f[I]\(ti/.config/vinput/config.json\f[R] \(em
核心配置，涵盖音频采集、ASR 引擎、LLM 服务商及后处理场景。
.IP "2." 3
\f[I]\(ti/.config/fcitx5/conf/vinput.conf\f[R] \(em Fcitx5
插件配置，涵盖快捷键及界面显示设置。
.SH CONFIG.JSON 结构
核心配置文件采用 JSON
格式，主要包含四个顶级字段：\f[B]global\f[R]、\f[B]asr\f[R]、\f[B]llm\f[R]
与 \f[B]scenes\f[R]。
.SS GLOBAL 全局配置
控制底层音频环境与录音行为。
.IP
.EX
\(dqglobal\(dq\f[B]:\f[R] {
  \(dqdefault_language\(dq: \(dqzh\(dq,
  \(dqcapture_device\(dq: \(dqdefault\(dq,
  \(dqduck_output_while_recording\(dq: \f[B]false\f[R],
  \(dqduck_output_volume\(dq: 0.25
}
.EE
.TP
\f[B]default_language\f[R] (\f[I]字符串\f[R])
默认语言代码（例如 \f[CR]\(dqzh\(dq\f[R], \f[CR]\(dqen\(dq\f[R]）。
.TP
\f[B]capture_device\f[R] (\f[I]字符串\f[R])
PipeWire 音频采集节点名称，或 \f[CR]\(dqdefault\(dq\f[R]
使用系统默认输入设备。
.TP
\f[B]duck_output_while_recording\f[R] (\f[I]布尔值\f[R])
是否在录音时通过 WirePlumber 自动降低系统扬声器音量。
.TP
\f[B]duck_output_volume\f[R] (\f[I]浮点数\f[R], \f[CR]0.0\f[R] \- \f[CR]1.0\f[R])
降低音量时的目标音量比例（\f[CR]0.25\f[R] 表示降至原音量的 25%）。
.SS ASR 语音识别配置
配置本地与云端语音识别引擎。
.IP
.EX
\(dqasr\(dq\f[B]:\f[R] {
  \(dqactive_provider\(dq: \(dqsherpa\-onnx\(dq,
  \(dqnormalize_audio\(dq: \f[B]true\f[R],
  \(dqinput_gain\(dq: 1.0,
  \(dqvad\(dq: {
    \(dqenabled\(dq: \f[B]true\f[R],
    \(dqthreshold\(dq: 0.5,
    \(dqmin_speech_duration\(dq: 0.15,
    \(dqspeech_pad_ms\(dq: 300
  },
  \(dqproviders\(dq: [ \f[B]...\f[R] ]
}
.EE
.TP
\f[B]active_provider\f[R] (\f[I]字符串\f[R])
当前激活的 ASR 服务商 ID。本地离线识别使用
\f[CR]\(dqsherpa\-onnx\(dq\f[R]。
.TP
\f[B]normalize_audio\f[R] (\f[I]布尔值\f[R])
是否在识别前对输入音频波形进行归一化。
.TP
\f[B]input_gain\f[R] (\f[I]浮点数\f[R])
软件录音增益倍数。
.TP
\f[B]vad.enabled\f[R] (\f[I]布尔值\f[R])
是否启用静音检测，自动过滤首尾无效静音。
.SS LLM 服务商配置
定义 OpenAI 兼容的 API 端点。
.IP
.EX
\(dqllm\(dq\f[B]:\f[R] {
  \(dqproviders\(dq: [
    {
      \(dqid\(dq: \(dqgroq\(dq,
      \(dqbase_url\(dq: \(dqhttps://api.groq.com/openai/v1\(dq,
      \(dqapi_key\(dq: \(dqYOUR_API_KEY\(dq,
      \(dqextra_body\(dq: {}
    }
  ],
  \(dqadapters\(dq: [
    {
      \(dqid\(dq: \(dqmy\-adapter\(dq,
      \(dqcommand\(dq: \(dqpython3\(dq,
      \(dqargs\(dq: [\(dq/path/to/entry.py\(dq],
      \(dqauto_start\(dq: \f[B]true\f[R]
    }
  ]
}
.EE
.TP
\f[B]adapters[].auto_start\f[R] (\f[I]布尔值\f[R])
是否在 \f[B]vinput\-daemon\f[R] 启动时自动启动该适配器进程。
.SS SCENES 场景后处理配置
定义用于文字润色、错别字修正或翻译的提示词模板与候选词配置。
.IP
.EX
\(dqscenes\(dq\f[B]:\f[R] {
  \(dqactive_scene\(dq: \(dq__raw__\(dq,
  \(dqdefinitions\(dq: [
    {
      \(dqid\(dq: \(dq__raw__\(dq,
      \(dqcount\(dq: 0
    },
    {
      \(dqid\(dq: \(dqdefault\(dq,
      \(dqlabel\(dq: \(dq默认润色\(dq,
      \(dqprompt\(dq: \(dq修正拼写与标点，保持原意不变。\(dq,
      \(dqprovider_id\(dq: \(dqgroq\(dq,
      \(dqmodel\(dq: \(dqllama\-3.3\-70b\-versatile\(dq,
      \(dqcontext_lines\(dq: 3,
      \(dqcount\(dq: 5
    }
  ]
}
.EE
.TP
\f[B]active_scene\f[R] (\f[I]字符串\f[R])
当前激活的场景 ID。\f[CR]__raw__\f[R] 表示跳过 LLM 后处理，直接输出 ASR
原始文本。
.TP
\f[B]context_lines\f[R] (\f[I]整数\f[R])
发送给 LLM 的历史上下文行数（\f[CR]0\f[R] 表示不附带上下文）。
.TP
\f[B]count\f[R] (\f[I]整数\f[R], \f[CR]0\f[R] \- \f[CR]9\f[R])
向 LLM 请求改写候选的最大数量（设为 \f[CR]0\f[R] 表示不调用
LLM）。改写结果与原始识别文本自动保序去重。去重后若只有单一结果直接自动上屏；若有不同候选则弹出菜单，默认聚焦
LLM 最佳改写，原始文本保留为保底选项（选项 1）。
.SH VINPUT.CONF 结构 (FCITX5 插件配置)
Fcitx5 插件的配置文件为 INI 格式，位于
\f[I]\(ti/.config/fcitx5/conf/vinput.conf\f[R]。
.IP
.EX
\f[B][Trigger]\f[R]
TriggerMode=Both
TriggerKey=Alt_R
CommandKeys=Control_R
SceneMenuKey=Shift_R
AsrMenuKey=F8
PagePrevKeys=Page_Up,KP_Page_Up
PageNextKeys=Page_Down,KP_Page_Down
MaxStreamingDisplayWidth=60
.EE
.TP
\f[B]TriggerMode\f[R] (\f[I]枚举值: Tap, Hold, Both\f[R])
按键触发模式。\f[CR]Tap\f[R] 短按切换开始/停止；\f[CR]Hold\f[R]
长按说话松开识别；\f[CR]Both\f[R] 短按长按均可。
.TP
\f[B]TriggerKey\f[R] (\f[I]按键列表\f[R])
触发语音录音的快捷键（默认：右 Alt）。
.TP
\f[B]CommandKeys\f[R] (\f[I]按键列表\f[R])
对选中文本执行划词语音指令的按键（默认：右 Control）。
.TP
\f[B]SceneMenuKey\f[R] (\f[I]按键列表\f[R])
打开后处理场景选择菜单的快捷键（默认：右 Shift）。
.TP
\f[B]AsrMenuKey\f[R] (\f[I]按键列表\f[R])
打开 ASR 语音识别引擎切换菜单的快捷键（默认：F8）。
.TP
\f[B]MaxStreamingDisplayWidth\f[R] (\f[I]整数\f[R], \f[CR]0\f[R] \- \f[CR]500\f[R])
实时流式识别预览的最大视觉显示列宽。超出时自动将较早文字折叠为\(lq句首...句尾\(rq。设为
\f[CR]0\f[R] 则禁用折叠（默认：\f[CR]60\f[R]）。
.SH 文件
.TP
\f[I]\(ti/.config/vinput/config.json\f[R]
用户核心配置文件。
.TP
\f[I]\(ti/.config/fcitx5/conf/vinput.conf\f[R]
Fcitx5 插件快捷键与界面配置文件。
.TP
\f[I]\(ti/.var/app/org.fcitx.Fcitx5/config/vinput/config.json\f[R]
在 Flatpak 沙箱环境下的配置文件路径。
.SH 另请参阅
\f[B]vinput\f[R](1), \f[B]vinput\-daemon\f[R](1), \f[B]fcitx5\f[R](1)
