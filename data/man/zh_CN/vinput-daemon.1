.\" Automatically generated by Pandoc 3.11
.\"
.TH "VINPUT\-DAEMON" "1" "2026年9月" "fcitx5\-vinput" "fcitx5\-vinput 用户手册"
.SH 名称
vinput\-daemon \- fcitx5\-vinput 后台语音识别与处理守护进程
.SH 语法
\f[B]vinput\-daemon\f[R]
.SH 描述
\f[B]vinput\-daemon\f[R] 是 \f[B]fcitx5\-vinput\f[R]
的后台核心服务组件，通过 D\-Bus（\f[CR]org.fcitx.Vinput\f[R]）与 Fcitx5
插件及 CLI 工具通信。
.PP
守护进程的主要职责包括：
.IP \(bu 2
通过 \f[B]PipeWire\f[R] 捕获麦克风输入音频；
.IP \(bu 2
基于 \f[B]sherpa\-onnx\f[R] 进行本地离线语音识别（ASR）；
.IP \(bu 2
支持流式及批处理转发至云端 ASR 服务商脚本；
.IP \(bu 2
基于 \f[B]VAD\f[R]（静音检测）过滤无效音频；
.IP \(bu 2
调度 OpenAI 兼容大模型进行场景后处理与重写；
.IP \(bu 2
管理后台 LLM 适配器进程的生命周期；
.IP \(bu 2
录音期间通过 \f[B]WirePlumber\f[R] 动态降低系统背景音量（Audio
Ducking）。
.SH 服务管理与生命周期
\f[B]vinput\-daemon\f[R] 作为 systemd 用户服务运行，亦支持 D\-Bus
按需激活。
.TP
启动服务:
\f[CR]systemctl \-\-user start vinput\-daemon.service\f[R]
.TP
设置登录自启并立即运行:
\f[CR]systemctl \-\-user enable \-\-now vinput\-daemon.service\f[R]
.TP
查看运行日志:
\f[CR]journalctl \-\-user \-u vinput\-daemon.service \-f\f[R]
.SH 环境变量
.TP
\f[B]VINPUT_CAPTURE_REUSE\f[R]
控制录音结束后是否在短时间内保持已连接但未激活的 PipeWire
音频流（默认值：\f[CR]1\f[R]）。该热路径（Warm
Path）机制显著降低了下一次触发录音时的启动延迟。设置为 \f[CR]0\f[R]
则每次停止录音后立即销毁音频流。
.TP
\f[B]VINPUT_CAPTURE_IDLE_DESTROY_MS\f[R]
闲置超时时间（毫秒），超时后销毁未激活的 PipeWire
音频流（默认值：\f[CR]15000\f[R]，即 15 秒）。
.TP
\f[B]VINPUT_DEBUG\f[R]
设置为 \f[CR]1\f[R] 可开启详细的调试日志输出。
.TP
\f[B]VINPUT_CONFIG_PATH\f[R]
自定义 \f[CR]config.json\f[R] 的配置文件路径。
.SH 隐私与音频安全
当音频热路径（Warm
Path）处于非活跃状态时，\f[B]绝不会读取或向内存缓冲任何音频数据\f[R]。闲置期过后，音频采集流将被完全关闭与销毁。
.SH 文件
.TP
\f[I]\(ti/.config/vinput/config.json\f[R]
守护进程核心配置文件。
.TP
\f[I]/usr/share/systemd/user/vinput\-daemon.service\f[R]
Systemd 用户服务单元文件。
.TP
\f[I]/usr/share/dbus\-1/services/org.fcitx.Vinput.service\f[R]
D\-Bus 激活配置文件。
.SH 另请参阅
\f[B]vinput\f[R](1), \f[B]vinput\-config\f[R](5), \f[B]fcitx5\f[R](1),
\f[B]systemctl\f[R](1), \f[B]pipewire\f[R](1)
